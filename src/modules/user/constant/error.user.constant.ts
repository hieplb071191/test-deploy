export enum UserError {
    USER_NOT_FOUND = 'user.not.found',
    USER_EXISTED = 'user.existed',
    USER_NOT_COMPARE_PASSWORD = 'user.not.compare.password',
    USER_FORBIDDEN_ACCESS_RESOURCE = 'user.forbidden.access.resource'
}